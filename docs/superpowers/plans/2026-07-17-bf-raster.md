# BF Raster Printing Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan.

**Goal:** Generate printable CTP500 `BF` frames from arbitrary images.

**Architecture:** Keep image preparation, BF encoding, and gatttool transport separate. Add a BF encoder module, route normal printing through it, and retain PacketLogger replay for comparison.

**Tech Stack:** Python 3, Pillow, pytest, direct BlueZ gatttool.

---

## Chunk 1: Capture-derived encoder

### Task 1: Lock byte behavior with tests

**Files:**
- Create: `tests/test_ctp500_bf.py`
- Reference: `ctp500_pklg.py`

- [ ] Add tests for frame prefix, little-endian payload length, checksum/trailer, deterministic output, and a tiny synthetic bitmap.
- [ ] Run `pytest tests/test_ctp500_bf.py -q`; confirm failure because the encoder is absent.

### Task 2: Implement BF encoding

**Files:**
- Create: `ctp500_bf.py`

- [ ] Implement the capture-derived row/segment encoder with explicit dimension and payload validation.
- [ ] Run the focused tests and confirm they pass.

## Chunk 2: CLI integration

### Task 3: Use BF frames for normal image printing

**Files:**
- Modify: `print.py`
- Modify: `tests/test_print_cli.py`

- [ ] Update normal printing to call the BF encoder.
- [ ] Keep `--replay-pklg` unchanged.
- [ ] Add CLI mock assertions for BF frames.
- [ ] Run the full suite: `pytest -q`.

### Task 4: Update documentation

**Files:**
- Modify: `README.md`

- [ ] Document BF image printing, preserved aspect ratio, and capture replay.
- [ ] Run `python3 -m compileall .` and the full test suite.
